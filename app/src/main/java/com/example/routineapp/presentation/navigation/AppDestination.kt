package com.example.routineapp.presentation.navigation

enum class AppDestination(val route: String, val label: String) {
    Today("today", "Today"),
    Routines("routines", "Routines"),
    History("history", "History"),
    Settings("settings", "Settings")
}
